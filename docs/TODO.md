# TODO LIST

- [ ] The `ComponentSpecStruct` struct does not get populated in `repositories.CreateRepositories()`
- [ ] `repo reindex $REPOSITORY` should use the repositories endpoints instead of the tasks ones